wow jfdklsajfdklsahjfkldajhglaikds; kdlsa
  dsa i i i jkd q q Qdefafdqdsfdsq() a;kfafass
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc --> shift :q
if found in vim, use  esc -->  hello wow this is cool :q
if found in vim, use  esc -->  hello wow this is cool :q

updated